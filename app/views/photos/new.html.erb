<h1 class="page-title"> Upload photos for <%= @plant.name %> <a class="btn btn-submit" href="<%= gardens_path %>"><span class="glyphicon glyphicon-th-large"></span> Back to Plant</a> </h1>
<div class="signup">
  <%= form_for @photo , {url: plant_photos_path(@plant)} do |f| %>
  <%= f.file_field :image, class: "photo-upload" %>
  <%= f.text_field :tags, placeholder: "Add Photo Tags" %>
  <%= f.submit "Submit", class: "btn btn-submit" %>
  <% end %>
</div>

