<% if @user.avatar? %>
  <%= image_tag @user.avatar.url, size: "100x100", class: "img-responsive thumbnail"%>
<% else %>
  <%= image_tag "default-avatar.png", size: "100x100", class: "img-responsive thumbnail"%>
<% end %>