 <nav class="navbar navbar-default navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">

      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#nav-right">
        <span class="sr-only"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    <a><%= link_to (image_tag "white_leaf.png", size: "25x25"), root_path, class: "navbar-brand pull-left" %></a>
    <% if current_user %>
      <a class="navbar-brand pull-right" href="<%= gardens_path %>">WELL ROOTED</a>
    <% else %>
      <a class="navbar-brand pull-right" href="<%= new_session_path %>">WELL ROOTED</a>
    <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="nav-right">
      <ul class="nav navbar-nav">
<!-- 
        <li><a href="#">Home</a></li>
        <li><a href="#meet">Gardens</a></li>
        <li><a href="#houses">Plants</a></li> -->
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="<%= gardens_path %>">Home</a></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name if current_user%><span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
          
          <% if (!current_user || @is_login == true) %>
            <li><a><%= link_to "Log in", new_session_path, method: :get %></a></li>
          <% end %>
            <li><a href="#">My Planter Boxes</a></li>
            <li><a href="<%= user_path(current_user) if current_user%>">Profile</a></li>
            <li><a href="#">Preferences</a></li>

            <li class="divider"></li>
            <li><a><%= link_to "Log out", session_path, method: :delete if current_user%> </a></li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
  </nav>
