<h1>My <%= @garden.name %></h1>

<p>Plants in my garden:</p>
<ul>
  <% @garden.plants.each do |p| %>
  <li><%= link_to p.name, garden_plant_path(@garden, p.id) %></li>
  <% end %>
</ul>
<%= link_to "Add new plant", new_garden_plant_path(@garden), class: "btn btn-primary"  %>

<%= link_to "Back to all gardens", gardens_path, class: "btn btn-primary"  %>